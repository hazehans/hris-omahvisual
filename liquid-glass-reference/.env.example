# Leave empty unless your app talks to a backend.
# Example: http://localhost:8000
VITE_API_BASE_URL=

# Optional: override sessionStorage key for the random stage background.
# Useful when multiple liquid-glass apps share one origin.
# VITE_BG_SESSION_KEY=my-app-bg-image
