# ======================================================
# HRIS OmahVisual - Backend Dependencies
# Django 5.1 (supports Python 3.13)
# ======================================================

# --- Core Framework ---
Django==5.1.4
djangorestframework==3.15.2

# --- Authentication ---
djangorestframework-simplejwt==5.3.1

# --- Database ---
# SQLite dipakai untuk development (switch ke psycopg2 saat Docker/PostgreSQL siap)
# psycopg2-binary==2.9.9   # Uncomment saat production (PostgreSQL)

# --- CORS ---
django-cors-headers==4.4.0

# --- Environment Variables ---
django-environ==0.11.2

# --- Image/File Handling ---
Pillow==10.4.0

# --- Celery (Task Scheduler) ---
celery==5.4.0
django-celery-beat==2.7.0          # Cron job scheduler
django-celery-results==2.5.1       # Pakai DB sebagai broker (sementara, sebelum Redis siap)

# --- Utilities ---
python-dateutil==2.9.0

